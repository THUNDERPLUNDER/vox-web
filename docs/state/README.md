# VIS state channel

`vis-state` is the deploy-independent projection channel for explicitly approved VIS state artifacts.

Authority remains Project Brain + explicit Thomas approval. This branch/file channel is a machine-readable projection, not a new source of strategy. Application code must not be developed here.

Current roadmap artifact: `docs/state/vis-roadmap-projection-v02.json`.
