---
/* Preview-only decision gate for #121. All derivatives reuse locked canonical geometry. */
const base = import.meta.env.BASE_URL;

const wordmarks = [
  { id: "primary", label: "Primary", detail: "5 stolper · fra 22 px", width: 937.88, height: 134, renderHeight: 32 },
  { id: "small", label: "Small Y", detail: "5 stolper · 18–20 px", width: 140, height: 20, renderHeight: 20 },
  { id: "micro", label: "Micro A", detail: "3 stolper · 8–16 px", width: 140, height: 20, renderHeight: 16 },
];

const treatments = [
  { id: "default", label: "Default", detail: "Navy + blue", dark: false },
  { id: "mono-navy", label: "Mono dark", detail: "Alt i navy", dark: false },
  { id: "inverse-blue", label: "Inverse", detail: "Hvit + blue", dark: true },
  { id: "mono-white", label: "Mono white", detail: "Alt i hvitt", dark: true },
];

const wordmarkSrc = (wordmark: (typeof wordmarks)[number], treatment: (typeof treatments)[number]) =>
  treatment.id === "default"
    ? `${base}brand/viddel-wordmark-${wordmark.id}-${wordmark.id === "micro" ? "3bar" : "5bar"}.svg`
    : `${base}brand/preview/viddel-wordmark-${wordmark.id}-${treatment.id}.svg`;

const signals = [
  { id: "primary", label: "Primary 5-bar", detail: "uttrukket fra Primary", aspect: 108.57 / 93.73 },
  { id: "small", label: "Small 5-bar", detail: "uttrukket fra Small Y", aspect: 18 / 16.7414 },
  { id: "micro", label: "Micro 3-bar", detail: "uttrukket fra Micro A", aspect: 10 / 16.7414 },
];
const signalSizes = [8, 12, 16, 20, 24, 32];
---

<section class="brand-review" aria-labelledby="brand-review-heading">
  <header class="brand-review__head">
    <div>
      <p>#121 · neste beslutningsport</p>
      <h2 id="brand-review-heading">Fargebehandlinger og standalone-signal</h2>
    </div>
    <p>
      Geometrien er låst. Variantene under er mekanisk avledet fra de tre canonical wordmarkene;
      ingen punkter, proporsjoner eller mellomrom er endret.
    </p>
  </header>

  <div class="brand-review__matrix" aria-label="Wordmark-fargebehandlinger">
    {wordmarks.map((wordmark) => (
      <article class="wordmark-row">
        <header>
          <strong>{wordmark.label}</strong>
          <span>{wordmark.detail}</span>
        </header>
        <div class="wordmark-row__treatments">
          {treatments.map((treatment) => (
            <figure class:list={["treatment", treatment.dark && "treatment--dark"]}>
              <div class="treatment__stage">
                <img
                  src={wordmarkSrc(wordmark, treatment)}
                  alt={`VIDDEL ${wordmark.label}, ${treatment.detail}`}
                  width={wordmark.width}
                  height={wordmark.height}
                  style={`height: ${wordmark.renderHeight}px`}
                />
              </div>
              <figcaption><strong>{treatment.label}</strong><span>{treatment.detail}</span></figcaption>
            </figure>
          ))}
        </div>
      </article>
    ))}
  </div>

  <div class="signal-review">
    <header class="signal-review__head">
      <div>
        <p>Standalone · stress-test</p>
        <h3>Tre signalgeometrier ved 8–32 px</h3>
      </div>
      <p>Vurder separasjon, rytme og om symbolet fortsatt oppleves som lyd – ikke bare streker.</p>
    </header>

    <div class="signal-review__grid">
      {signals.map((signal) => (
        <article class="signal-card">
          <header><strong>{signal.label}</strong><span>{signal.detail}</span></header>
          <div class="signal-card__sizes">
            {signalSizes.map((size) => (
              <figure>
                <div class="signal-card__stage">
                  <span
                    class="signal-shape"
                    role="img"
                    aria-label={`${signal.label} ved ${size} piksler`}
                    style={`--signal-url: url("${base}brand/preview/viddel-signal-${signal.id}.svg"); --signal-height: ${size}px; --signal-aspect: ${signal.aspect}; --signal-color: #3672e6;`}
                  ></span>
                </div>
                <figcaption>{size} px</figcaption>
              </figure>
            ))}
          </div>
          <div class="signal-card__contexts">
            <div class="signal-icon signal-icon--light" aria-label={`${signal.label}, blå på lys flate`}>
              <span class="signal-shape" style={`--signal-url: url("${base}brand/preview/viddel-signal-${signal.id}.svg"); --signal-height: 24px; --signal-aspect: ${signal.aspect}; --signal-color: #3672e6;`}></span>
            </div>
            <div class="signal-icon signal-icon--dark" aria-label={`${signal.label}, blå på mørk flate`}>
              <span class="signal-shape" style={`--signal-url: url("${base}brand/preview/viddel-signal-${signal.id}.svg"); --signal-height: 24px; --signal-aspect: ${signal.aspect}; --signal-color: #3672e6;`}></span>
            </div>
            <div class="signal-icon signal-icon--dark" aria-label={`${signal.label}, hvit på mørk flate`}>
              <span class="signal-shape" style={`--signal-url: url("${base}brand/preview/viddel-signal-${signal.id}.svg"); --signal-height: 24px; --signal-aspect: ${signal.aspect}; --signal-color: #ffffff;`}></span>
            </div>
          </div>
        </article>
      ))}
    </div>
  </div>

  <footer class="brand-review__decision">
    <strong>Beslutning etter review:</strong> godkjenn fargebehandlingene og velg hvilken 5-bar-geometri
    som skal være standalone-master. Favicon avgjøres først etter dette.
  </footer>
</section>

<style>
  .brand-review {
    display: grid;
    gap: 1rem;
    margin-top: 1rem;
    padding: clamp(1rem, 2vw, 1.5rem);
    border: 1px solid #d9dee7;
    border-radius: 1rem;
    background: #f8fafc;
    color: #0f1b2e;
  }

  .brand-review__head,
  .signal-review__head {
    display: grid;
    gap: 0.75rem;
  }

  .brand-review__head p,
  .signal-review__head p,
  .brand-review__head h2,
  .signal-review__head h3 {
    margin: 0;
  }

  .brand-review__head > div > p,
  .signal-review__head > div > p {
    color: #3672e6;
    font-size: 0.66rem;
    font-weight: 800;
    letter-spacing: 0.14em;
    text-transform: uppercase;
  }

  .brand-review__head h2 {
    margin-top: 0.25rem;
    font-size: clamp(1.25rem, 2.5vw, 1.75rem);
  }

  .brand-review__head > p,
  .signal-review__head > p {
    max-width: 36rem;
    color: #667085;
    font-size: 0.78rem;
    line-height: 1.55;
  }

  .brand-review__matrix,
  .signal-review,
  .signal-review__grid {
    display: grid;
    gap: 0.85rem;
  }

  .wordmark-row,
  .signal-card {
    overflow: hidden;
    border: 1px solid #d9dee7;
    border-radius: 0.75rem;
    background: #fff;
  }

  .wordmark-row > header,
  .signal-card > header {
    display: flex;
    align-items: baseline;
    justify-content: space-between;
    gap: 1rem;
    padding: 0.6rem 0.75rem;
    border-bottom: 1px solid #d9dee7;
    font-size: 0.72rem;
  }

  .wordmark-row > header span,
  .signal-card > header span {
    color: #667085;
  }

  .wordmark-row__treatments {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }

  .treatment {
    min-width: 0;
    margin: 0;
    border-top: 1px solid #d9dee7;
  }

  .treatment:nth-child(-n + 2) { border-top: 0; }
  .treatment:nth-child(even) { border-left: 1px solid #d9dee7; }

  .treatment__stage {
    display: flex;
    min-height: 6rem;
    align-items: center;
    justify-content: center;
    padding: 1rem;
    background: #fff;
  }

  .treatment--dark .treatment__stage {
    background: #0f1b2e;
  }

  .treatment img {
    display: block;
    width: auto;
    max-width: 100%;
  }

  .treatment figcaption {
    display: flex;
    justify-content: space-between;
    gap: 0.5rem;
    padding: 0.5rem 0.65rem;
    border-top: 1px solid #d9dee7;
    color: #667085;
    font-size: 0.66rem;
  }

  .treatment figcaption strong { color: #0f1b2e; }

  .signal-review {
    margin-top: 0.25rem;
    padding-top: 1rem;
    border-top: 1px solid #d9dee7;
  }

  .signal-review__head h3 {
    margin-top: 0.25rem;
    font-size: 1.1rem;
  }

  .signal-card__sizes {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
  }

  .signal-card__sizes figure {
    min-width: 0;
    margin: 0;
    border-top: 1px solid #e8ebf0;
    border-left: 1px solid #e8ebf0;
  }

  .signal-card__sizes figure:nth-child(-n + 3) { border-top: 0; }
  .signal-card__sizes figure:nth-child(3n + 1) { border-left: 0; }

  .signal-card__stage {
    display: grid;
    min-height: 4.5rem;
    place-items: center;
    background-image:
      linear-gradient(rgb(15 27 46 / 0.045) 1px, transparent 1px),
      linear-gradient(90deg, rgb(15 27 46 / 0.045) 1px, transparent 1px);
    background-size: 8px 8px;
  }

  .signal-card__sizes figcaption {
    padding: 0.35rem;
    border-top: 1px solid #e8ebf0;
    color: #667085;
    font-size: 0.64rem;
    text-align: center;
  }

  .signal-shape {
    display: block;
    width: calc(var(--signal-height) * var(--signal-aspect));
    height: var(--signal-height);
    background: var(--signal-color);
    -webkit-mask: var(--signal-url) center / contain no-repeat;
    mask: var(--signal-url) center / contain no-repeat;
  }

  .signal-card__contexts {
    display: flex;
    gap: 0.6rem;
    justify-content: center;
    padding: 0.75rem;
    border-top: 1px solid #d9dee7;
    background: #f8fafc;
  }

  .signal-icon {
    display: grid;
    width: 3.5rem;
    height: 3.5rem;
    place-items: center;
    border: 1px solid #d9dee7;
    border-radius: 0.9rem;
  }

  .signal-icon--light { background: #fff; }
  .signal-icon--dark { background: #0f1b2e; border-color: #24324a; }

  .brand-review__decision {
    padding: 0.8rem 0.9rem;
    border-radius: 0.7rem;
    background: #eaf1ff;
    color: #475467;
    font-size: 0.76rem;
    line-height: 1.5;
  }

  .brand-review__decision strong { color: #0f1b2e; }

  @media (min-width: 760px) {
    .brand-review__head,
    .signal-review__head {
      grid-template-columns: minmax(0, 1fr) minmax(18rem, 0.8fr);
      align-items: start;
    }

    .signal-review__grid {
      grid-template-columns: repeat(3, minmax(0, 1fr));
    }
  }

  @media (max-width: 520px) {
    .wordmark-row__treatments { grid-template-columns: 1fr; }
    .treatment,
    .treatment:nth-child(-n + 2),
    .treatment:nth-child(even) { border-top: 1px solid #d9dee7; border-left: 0; }
    .treatment:first-child { border-top: 0; }
    .treatment__stage { justify-content: flex-start; }
  }
</style>
