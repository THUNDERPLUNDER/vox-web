---
import BaseLayout from "../../../layouts/BaseLayout.astro";
import Section from "../../../components/layout/Section.astro";
import Footer from "../../../components/nav/Footer.astro";

export async function getStaticPaths() {
  // TODO: Replace with real term source when glossary data is available.
  return [];
}

Astro.response.headers.set("X-Robots-Tag", "noindex, follow");

const { term } = Astro.params;
---

<BaseLayout title={`VOX - Ordbok: ${term ?? ""}`} currentPath="/no/ordbok">
  <meta slot="head" name="robots" content="noindex,follow" />

  <main class="py-8 sm:py-10">
    <Section>
      <h1 class="text-3xl font-semibold tracking-tight sm:text-4xl">
        Ordbok: {term}
      </h1>
      <p class="mt-4 text-[rgb(var(--muted))]">
        Placeholder: Oppslagsside for valgt term.
      </p>
    </Section>
  </main>

  <Footer />
</BaseLayout>
