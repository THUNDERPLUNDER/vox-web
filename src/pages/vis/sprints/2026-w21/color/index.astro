---
/* CONTRACT: VIS-only Color Lab for #122 — restrained signal system review.
   Parent: #126 sprint preview / #120 Design Lock. Not production design.
   Typography premiss from #123: Epilogue display proxy + Inter body/UI (no new fonts).
*/
import PrototypeLayout from "../../../../../layouts/PrototypeLayout.astro";
import "../../../../../styles/global.css";

const base = import.meta.env.BASE_URL;
const sprintPath = `${base}vis/sprints/2026-w21`;

const evalCriteria = [
  "Lesbarhet",
  "Tillit",
  "Viddel-særpreg",
  "Hverdagsenergi",
  "Ikke-spa / ikke-wellness",
  "UI-kontroll",
  "Signal restraint",
];

const articleContent = {
  kicker: "BESLUTNINGSSTØTTE",
  h1: "Audiograf: når det haster, og hva du kan forberede",
  ingress:
    "Noen høreapparatproblemer kan du feilsøke rolig selv. Andre bør vurderes raskt.",
};

const aiTransition = {
  label: "ETTER LESING",
  title: "Hva bør jeg gjøre nå?",
  text: "Beskriv hva som ikke fungerer, så hjelper Viddel deg videre.",
  button: "Få hjelp nå",
};

const heroContent = {
  headline: "Få verden tydeligere tilbake.",
  subline:
    "Viddel hjelper deg å forstå, feilsøke og mestre høreapparatet i hverdagen.",
};

const colorSets = [
  {
    id: "a",
    anchor: "set-a",
    label: "SET A",
    name: "Dyp Nordisk",
    role: "Trygg, autoritær, nordisk og helsefaglig forankret uten å bli klinisk.",
    tokens: [
      { key: "background", label: "background", value: "#FFFFFF" },
      { key: "text", label: "text", value: "#111827" },
      { key: "surface", label: "surface", value: "#F3F4F6" },
      { key: "border", label: "border", value: "#E5E7EB" },
      { key: "primary", label: "primary signal", value: "#0F4C81" },
      { key: "secondary", label: "secondary/focus signal", value: "#0EA5E9" },
    ],
  },
  {
    id: "b",
    anchor: "set-b",
    label: "SET B",
    name: "Skarp Kontrast",
    role: "Høy kontrast, teknisk, presis, svært tydelig.",
    tokens: [
      { key: "background", label: "background", value: "#FFFFFF" },
      { key: "text", label: "text", value: "#000000" },
      { key: "surface", label: "surface", value: "#F4F5F7" },
      { key: "border", label: "border", value: "#D1D5DB" },
      { key: "primary", label: "primary signal", value: "#0055FF" },
      { key: "secondary", label: "secondary/success signal", value: "#10B981" },
    ],
  },
  {
    id: "c",
    anchor: "set-c",
    label: "SET C",
    name: "Monokrom + Varm Action",
    role: "Utfordrer helse-blå. Stram, monokrom base med varm aksent for handling.",
    tokens: [
      { key: "background", label: "background", value: "#FFFFFF" },
      { key: "text", label: "text", value: "#18181B" },
      { key: "surface", label: "surface", value: "#F4F4F5" },
      { key: "border", label: "border", value: "#E4E4E7" },
      { key: "primary", label: "primary signal", value: "#EA580C" },
    ],
  },
  {
    id: "d",
    anchor: "set-d",
    label: "SET D",
    name: "Deep Teal + Magenta Signal",
    role: "Magnific-inspirert utfordrer. Dyp blå/teal base og sterk magenta som energisk signal-linje — ikke creative AI platform.",
    tokens: [
      { key: "background", label: "background", value: "#FFFFFF" },
      { key: "text", label: "text", value: "#111827" },
      { key: "surface", label: "surface", value: "#F3F4F6" },
      { key: "border", label: "border", value: "#E5E7EB" },
      { key: "primary", label: "primary deep signal", value: "#0F4C5C" },
      { key: "secondary", label: "kinetic signal", value: "#EC4899" },
      { key: "tertiary", label: "warm energy (optional)", value: "#F97316" },
    ],
    signalNote: "Magenta used as line/marker only — not fill or gradient.",
  },
];

const hierarchyBar = [
  {
    id: "light",
    label: "White / Clean Light Gray",
    share: "52%",
    color: "#FFFFFF",
    text: "#111827",
    border: true,
    flex: 52,
  },
  {
    id: "deep",
    label: "Deep Nordic Blue / Teal",
    share: "30%",
    color: "#0F4C5C",
    text: "#FFFFFF",
    flex: 30,
  },
  {
    id: "ink",
    label: "Near Black / Ink",
    share: "12%",
    color: "#111827",
    text: "#FFFFFF",
    flex: 12,
  },
  {
    id: "cyan",
    label: "Cyan Focus",
    share: "2%",
    color: "#0EA5E9",
    text: "#FFFFFF",
    flex: 2,
  },
  {
    id: "magenta",
    label: "Kinetic Magenta",
    share: "1.5%",
    color: "#EC4899",
    text: "#FFFFFF",
    flex: 1.5,
  },
];

const hierarchyParked = [
  { label: "Warm", color: "#F97316" },
  { label: "Success", color: "#10B981" },
];

const hierarchyUsage = [
  {
    name: "Deep Nordic Blue / Teal",
    role: "Primary identity depth, dark surfaces, chrome moments, hero contrast.",
    share: "25–35%",
    doUse: "Dark hero, chrome/nav, dark AI transition, footer/dark editorial bands.",
    doNot: "Decorative fills across reading surfaces or large layout blocks.",
    colors: "#0F4C81 · #0F4C5C · #071827",
  },
  {
    name: "White / Clean Light Gray",
    role: "Editorial base, reading space, help space.",
    share: "45–60%",
    doUse: "Page background, article surfaces, content + AI help layer.",
    doNot: "Tinted wellness base or cream/powder editorial wash.",
    colors: "#FFFFFF · #F3F4F6 · #F8FAFC",
  },
  {
    name: "Near Black / Ink",
    role: "Typography, monochrome UI, primary buttons.",
    share: "10–15%",
    doUse: "Text, buttons, UI anchors, structural monochrome actions.",
    doNot: "Large colored UI blocks or signal replacement.",
    colors: "#111827 · #020617 · #18181B",
  },
  {
    name: "Cyan Focus",
    role: "Functional focus, active link, precise interaction signal.",
    share: "1–3%",
    doUse: "Focus rings, active link, small UI state.",
    doNot: "Brand hero fill or decorative accent bands.",
    colors: "#0EA5E9",
  },
  {
    name: "Kinetic Magenta",
    role: "Ghost signal, motion/sound marker, small energy pulse.",
    share: "under 1–2%",
    doUse: "Audio bars, small dots, subtle halo, dark-surface ghost outline.",
    doNot: "Long lines, large fills, gradients, decorative pink wash.",
    colors: "#EC4899 · muted tint optional",
  },
  {
    name: "Warm / Success",
    role: "System or future support colors, not core identity yet.",
    share: "parked / optional",
    doUse: "Only if state semantics require it later.",
    doNot: "Core brand identity or primary action color in hybrid track.",
    colors: "#F97316 · #EA580C · #10B981",
    parked: true,
  },
];
---

<PrototypeLayout title="Color Lab — #122">
  <main class="clab" data-color-lab>
    <nav class="clab__breadcrumb" aria-label="Du er her">
      <a href={`${base}vis`}>VIS</a>
      <span aria-hidden="true">/</span>
      <a href={sprintPath}>2026-W21</a>
      <span aria-hidden="true">/</span>
      <span>Color</span>
    </nav>

    <header class="clab__hero">
      <p class="clab__kicker">VIS-only · #122</p>
      <h1>Color Lab — #122</h1>
      <p class="clab__subtitle">Monochrome base + restrained signal systems</p>
      <p class="clab__note">
        Color is under evaluation. This lab tests restrained signal systems in the W21 VIS preview.
        No production tokens are changed.
      </p>

      <dl class="clab__meta">
        <div><dt>Sprint</dt><dd>2026-W21</dd></div>
        <div><dt>Parent</dt><dd>#120</dd></div>
        <div><dt>Preview</dt><dd>#126</dd></div>
        <div><dt>Issue</dt><dd>#122</dd></div>
        <div><dt>Status</dt><dd>Open evaluation</dd></div>
      </dl>

      <div class="clab__criteria">
        <p class="clab__criteria-label">Evaluation criteria</p>
        <ul class="clab__criteria-list" role="list">
          {evalCriteria.map((c) => <li>{c}</li>)}
        </ul>
      </div>
    </header>

    <div class="clab__sets">
      {colorSets.map((set) => (
        <article id={set.anchor} class="clab__set" data-set={set.id}>
          <header class="clab__set-head">
            <p class="clab__set-label">{set.label}</p>
            <h2>{set.name}</h2>
            <p class="clab__set-role">{set.role}</p>
            {set.signalNote && <p class="clab__set-note">{set.signalNote}</p>}
          </header>

          <!-- 1. Token swatches -->
          <section class="clab__panel" aria-label="Token swatches">
            <p class="clab__panel-label">Token swatches</p>
            <ul class="clab__swatches" role="list">
              {set.tokens.map((token) => (
                <li class="clab__swatch">
                  <div
                    class="clab__swatch-color"
                    style={`background: ${token.value}`}
                    aria-hidden="true"
                  />
                  <div class="clab__swatch-meta">
                    <strong>{token.label}</strong>
                    <code>{token.value}</code>
                  </div>
                </li>
              ))}
            </ul>
          </section>

          <div class="clab__samples">
            <!-- 2. Article intro -->
            <section class="clab__panel clab__article" aria-label="Article intro">
              <p class="clab__panel-label">Article intro</p>
              <p class="clab__kicker-sample">{articleContent.kicker}</p>
              <h3 class="clab__h1">{articleContent.h1}</h3>
              <p class="clab__ingress">{articleContent.ingress}</p>
            </section>

            <!-- 3. AI transition block -->
            <section class="clab__panel clab__ai" aria-label="AI transition block">
              <p class="clab__panel-label">AI transition block</p>
              <p class="clab__ai-label">{aiTransition.label}</p>
              <p class="clab__ai-title">{aiTransition.title}</p>
              <p class="clab__ai-text">{aiTransition.text}</p>
              <button type="button" class="clab__btn clab__btn--primary">
                {aiTransition.button}
              </button>
            </section>

            <!-- 4. UI primitives -->
            <section class="clab__panel clab__ui" aria-label="UI primitives">
              <p class="clab__panel-label">UI primitives</p>
              <div class="clab__ui-row">
                <button type="button" class="clab__btn clab__btn--primary">Primary button</button>
                <button type="button" class="clab__btn clab__btn--secondary">Secondary button</button>
              </div>
              <div class="clab__ui-row">
                <a href="#" class="clab__link" onclick="return false">Text link</a>
                <span class="clab__pill">Feilsøking</span>
              </div>
              <input
                type="text"
                class="clab__input"
                placeholder="Beskriv problemet kort …"
                aria-label="Input sample"
              />
            </section>

            <!-- 5. Signal marker -->
            <section class="clab__panel clab__marker" aria-label="Signal marker sample">
              <p class="clab__panel-label">Signal marker</p>
              <div class="clab__marker-demo">
                <span class="clab__marker-line" aria-hidden="true" />
                <div class="clab__marker-bars" aria-hidden="true">
                  <span /><span /><span /><span /><span />
                </div>
                <span class="clab__marker-dot" aria-hidden="true" />
              </div>
              <p class="clab__marker-caption">Signal used as line, bar or dot — not fill.</p>
            </section>

            <!-- 6. Mini hero light/dark -->
            <section class="clab__panel clab__hero-pair" aria-label="Mini hero preview">
              <p class="clab__panel-label">Mini hero light / dark</p>
              <div class="clab__hero-grid">
                <div class="clab__hero clab__hero--light">
                  <p class="clab__hero-tag">Light hero</p>
                  <p class="clab__hero-headline">{heroContent.headline}</p>
                  <p class="clab__hero-subline">{heroContent.subline}</p>
                </div>
                <div class="clab__hero clab__hero--dark">
                  <p class="clab__hero-tag">Dark hero</p>
                  <p class="clab__hero-headline">{heroContent.headline}</p>
                  <p class="clab__hero-subline">{heroContent.subline}</p>
                </div>
              </div>
            </section>
          </div>
        </article>
      ))}
    </div>

    <section class="clab__hybrid" aria-labelledby="hybrid-heading">
      <header class="clab__hybrid-head">
        <p class="clab__kicker">Recommended hybrid candidate</p>
        <h2 id="hybrid-heading">A+D — Dyp Nordisk + Kinetic Magenta</h2>
        <p>
          Combines the trust and Nordic authority of Set A with the kinetic signal energy of Set D.
          Magenta is used only as a restrained motion/sound marker, not as a decorative palette.
        </p>
        <p class="clab__hybrid-usage-note">
          Updated usage test: magenta is evaluated as a restrained ghost/micro-signal,
          especially on dark surfaces, rather than as a structural line color.
        </p>
      </header>

      <ul class="clab__hybrid-tokens" role="list" aria-label="Hybrid tokens">
        <li><strong>background</strong><code>#FFFFFF</code></li>
        <li><strong>text</strong><code>#111827</code></li>
        <li><strong>surface</strong><code>#F3F4F6</code></li>
        <li><strong>border</strong><code>#E5E7EB</code></li>
        <li><strong>primary deep</strong><code>#0F4C81 / #0F4C5C</code></li>
        <li><strong>signal magenta</strong><code>#EC4899</code></li>
        <li><strong>focus cyan</strong><code>#0EA5E9 optional</code></li>
        <li><strong>action button</strong><code>monochrome / near-black</code></li>
      </ul>

      <div class="clab__hybrid-rules">
        <div>
          <p class="clab__panel-label">Magenta allowed</p>
          <ul role="list">
            <li>ghost halo</li>
            <li>audio bars</li>
            <li>focus accent</li>
            <li>tiny dot/marker</li>
            <li>short edge/transition indicator</li>
          </ul>
        </div>
        <div>
          <p class="clab__panel-label">Magenta not allowed</p>
          <ul role="list">
            <li>long structural line</li>
            <li>large filled button</li>
            <li>large background</li>
            <li>full gradient</li>
            <li>decorative pink wash</li>
          </ul>
        </div>
      </div>

      <div class="clab__hybrid-grid">
        <!-- 1. Chrome / navigation sample -->
        <section class="clab__hybrid-panel clab__hybrid-panel--wide" aria-label="Chrome navigation sample">
          <p class="clab__panel-label">Chrome / navigation sample</p>
          <div class="clab__mock-chrome">
            <a href="#" class="clab__wordmark" onclick="return false">VIDDEL</a>
            <nav class="clab__mock-nav" aria-label="Hybrid navigation sample">
              <a href="#" onclick="return false">Artikler</a>
              <a href="#" onclick="return false">Feilsøking</a>
              <a href="#" onclick="return false">Om Viddel</a>
            </nav>
            <button type="button" class="clab__mock-action">Få hjelp nå</button>
          </div>
        </section>

        <!-- 2. Editorial article start -->
        <section class="clab__hybrid-panel clab__hybrid-editorial" aria-label="Editorial article start">
          <p class="clab__panel-label">Editorial article start</p>
          <div class="clab__editorial-start">
            <span class="clab__edge-marker" aria-hidden="true"></span>
            <p class="clab__kicker-sample">{articleContent.kicker}</p>
            <h3 class="clab__h1">{articleContent.h1}</h3>
            <p class="clab__ingress">{articleContent.ingress}</p>
          </div>
        </section>

        <!-- 3. Article to AI transition block -->
        <section class="clab__hybrid-panel clab__hybrid-ai" aria-label="Article to AI transition block">
          <p class="clab__panel-label">Article → AI transition block</p>
          <div class="clab__transition-line" aria-hidden="true"></div>
          <p class="clab__ai-label">{aiTransition.label}</p>
          <p class="clab__ai-title">{aiTransition.title}</p>
          <p class="clab__ai-text">{aiTransition.text}</p>
          <div class="clab__ai-form">
            <input type="text" class="clab__input" placeholder="Beskriv problemet kort …" aria-label="Hybrid AI input sample" />
            <button type="button" class="clab__btn clab__btn--primary">Få hjelp nå</button>
          </div>
        </section>

        <!-- 4. Hub card cluster -->
        <section class="clab__hybrid-panel clab__hybrid-panel--wide" aria-label="Hub card cluster">
          <p class="clab__panel-label">Hub card cluster</p>
          <div class="clab__hub-cluster">
            <article class="clab__hub-card clab__hub-card--active">
              <span aria-hidden="true"></span>
              <h3>Få slutt på pipingen</h3>
              <p>Lyd som lekker tilbake kan ofte løses med enkle grep.</p>
            </article>
            <article class="clab__hub-card">
              <span aria-hidden="true"></span>
              <h3>Koble til mobilen</h3>
              <p>Sjekk app, Bluetooth og rekkefølge før du prøver på nytt.</p>
            </article>
            <article class="clab__hub-card">
              <span aria-hidden="true"></span>
              <h3>Bytte filter</h3>
              <p>Et tett filter kan gjøre lyden svakere eller uklar.</p>
            </article>
          </div>
        </section>

        <!-- 5. Photo / image placeholder card -->
        <section class="clab__hybrid-panel" aria-label="Photo energy placeholder">
          <p class="clab__panel-label">Photo / image placeholder card</p>
          <div class="clab__photo-card">
            <div class="clab__photo-placeholder" aria-hidden="true">
              <span></span>
            </div>
            <div class="clab__photo-copy">
              <p>photo energy placeholder</p>
              <strong>Everyday life carries warmth. Color stays controlled around it.</strong>
            </div>
          </div>
        </section>

        <!-- 6. Icon / signal system sample -->
        <section class="clab__hybrid-panel" aria-label="Icon and signal system sample">
          <p class="clab__panel-label">Icon / signal system sample</p>
          <div class="clab__signal-system">
            <div class="clab__hybrid-bars" aria-label="Audio bars">
              <span /><span /><span /><span /><span /><span />
            </div>
            <div class="clab__dot-sequence" aria-label="Dot sequence">
              <span /><span /><span /><span />
            </div>
            <span class="clab__short-line" aria-hidden="true"></span>
            <span class="clab__focus-ring" aria-label="Focus ring sample"></span>
            <span class="clab__ai-spark" aria-label="AI marker sample">AI</span>
            <button type="button" class="clab__ghost-pill">Ghost pill</button>
            <span class="clab__ghost-orb" aria-label="Ghost halo marker"></span>
          </div>
        </section>

        <!-- 7. Light / dark contrast strip -->
        <section class="clab__hybrid-panel clab__hybrid-panel--wide" aria-label="Light and dark contrast strip">
          <p class="clab__panel-label">Light / dark contrast strip</p>
          <div class="clab__contrast-strip">
            <div class="clab__contrast clab__contrast--light">
              <p class="clab__hero-tag">Light editorial base</p>
              <p class="clab__hero-headline">{heroContent.headline}</p>
              <p class="clab__hero-subline">{heroContent.subline}</p>
            </div>
            <div class="clab__contrast clab__contrast--dark">
              <p class="clab__hero-tag">Dark contrast test</p>
              <p class="clab__hero-headline">{heroContent.headline}</p>
              <p class="clab__hero-subline">{heroContent.subline}</p>
            </div>
          </div>
        </section>
      </div>
    </section>

    <section class="clab__hierarchy" aria-labelledby="hierarchy-heading">
      <header class="clab__hierarchy-head">
        <p class="clab__kicker">Color hierarchy</p>
        <h2 id="hierarchy-heading">Color hierarchy — proposed usage distribution</h2>
        <p>
          This hierarchy tests how the hybrid palette should be distributed across future Viddel surfaces.
          It is a usage model, not final token lock.
        </p>
      </header>

      <!-- A. Large hierarchy bar -->
      <section class="clab__hierarchy-bar-wrap" aria-label="Proposed color distribution bar">
        <p class="clab__panel-label">Proposed distribution v0.1</p>
        <div class="clab__hierarchy-bar">
          {hierarchyBar.map((segment) => (
            <div
              class="clab__hierarchy-segment"
              data-segment={segment.id}
              style={`--seg-flex: ${segment.flex}; --seg-bg: ${segment.color}; --seg-text: ${segment.text};`}
            >
              <span class="clab__hierarchy-segment-share">{segment.share}</span>
              <span class="clab__hierarchy-segment-label">{segment.label}</span>
            </div>
          ))}
        </div>
        <div class="clab__hierarchy-parked" aria-label="Parked optional system colors">
          {hierarchyParked.map((chip) => (
            <span class="clab__hierarchy-chip" style={`--chip-bg: ${chip.color}`}>{chip.label}</span>
          ))}
          <span class="clab__hierarchy-chip-note">parked / optional</span>
        </div>
      </section>

      <!-- B. Usage table -->
      <section class="clab__hierarchy-table-wrap" aria-label="Color usage table">
        <p class="clab__panel-label">Usage table</p>
        <div class="clab__hierarchy-table">
          {hierarchyUsage.map((row) => (
            <article class:list={["clab__hierarchy-row", row.parked && "clab__hierarchy-row--parked"]}>
              <header>
                <h3>{row.name}</h3>
                <p class="clab__hierarchy-share">{row.share}</p>
              </header>
              <p class="clab__hierarchy-role">{row.role}</p>
              <dl>
                <div><dt>Do</dt><dd>{row.doUse}</dd></div>
                <div><dt>Do not</dt><dd>{row.doNot}</dd></div>
                <div><dt>Hex</dt><dd><code>{row.colors}</code></dd></div>
              </dl>
            </article>
          ))}
        </div>
      </section>

      <!-- C. Applied miniature examples -->
      <section class="clab__hierarchy-mini-wrap" aria-label="Applied miniature examples">
        <p class="clab__panel-label">Applied miniature examples</p>
        <div class="clab__hierarchy-mini-grid">
          <article class="clab__hierarchy-mini clab__hierarchy-mini--dark-hero">
            <p class="clab__hierarchy-mini-label">1. Dark hero</p>
            <p class="clab__hierarchy-mini-headline">
              Få verden tydeligere tilbake.
              <span class="clab__hierarchy-ghost-dot" aria-hidden="true"></span>
            </p>
            <p class="clab__hierarchy-mini-copy">Deep blue/teal carries identity. Magenta stays a ghost pulse.</p>
          </article>

          <article class="clab__hierarchy-mini clab__hierarchy-mini--editorial">
            <p class="clab__hierarchy-mini-label">2. Editorial article</p>
            <p class="clab__hierarchy-mini-kicker">BESLUTNINGSSTØTTE</p>
            <p class="clab__hierarchy-mini-headline clab__hierarchy-mini-headline--display">
              Audiograf: når det haster
            </p>
            <p class="clab__hierarchy-mini-copy">Clean white reading base with deep blue micro-accent only.</p>
          </article>

          <article class="clab__hierarchy-mini clab__hierarchy-mini--ai">
            <p class="clab__hierarchy-mini-label">3. Article → AI</p>
            <p class="clab__hierarchy-mini-kicker">ETTER LESING</p>
            <p class="clab__hierarchy-mini-headline">Hva bør jeg gjøre nå?</p>
            <div class="clab__hierarchy-mini-signals" aria-hidden="true">
              <span class="clab__hierarchy-signal-dot clab__hierarchy-signal-dot--magenta"></span>
              <span class="clab__hierarchy-signal-dot clab__hierarchy-signal-dot--cyan"></span>
            </div>
            <button type="button" class="clab__hierarchy-mini-btn">Få hjelp nå</button>
          </article>
        </div>
      </section>

      <p class="clab__hierarchy-guardrail">
        Magenta should behave like a signal in the air, not a layout structure.
        Long lines, large fills and decorative gradients should remain avoided.
      </p>
    </section>

    <footer class="clab__footer">
      <p>
        Color direction is under evaluation. No production tokens changed in #122.
        Typography premiss from #123: Existing Bold Display — moderated + Inter for body/UI.
      </p>
      <p><a href={sprintPath}>← Back to Sprint Preview 2026-W21</a></p>
    </footer>
  </main>

  <style>
    [data-color-lab] {
      --clab-sans: "Inter", ui-sans-serif, system-ui, -apple-system, sans-serif;
      --clab-display: var(--font-display);

      min-height: 100vh;
      background: rgb(var(--article-page-canvas));
      color: rgb(var(--reading-ink));
      padding: 1.5rem 1rem 3rem;
      max-width: 72rem;
      margin: 0 auto;
      font-family: var(--clab-sans);
    }

    .clab__breadcrumb {
      display: flex;
      flex-wrap: wrap;
      gap: 0.45rem;
      align-items: center;
      margin-bottom: 1.1rem;
      font-size: 0.72rem;
      color: rgb(var(--reading-ink-secondary));
    }

    .clab__breadcrumb a {
      text-decoration: underline;
      text-underline-offset: 2px;
      color: inherit;
    }

    .clab__hero {
      padding: 1.5rem 0 2rem;
      border-bottom: 1px solid rgb(var(--border) / 0.24);
    }

    .clab__kicker {
      margin: 0;
      font-size: 0.68rem;
      font-weight: 700;
      letter-spacing: 0.16em;
      text-transform: uppercase;
      color: rgb(var(--reading-accent-iris));
    }

    .clab__hero > h1 {
      margin: 0.4rem 0 0;
      max-width: 52rem;
      font-family: var(--clab-display);
      font-size: clamp(1.85rem, 5vw, 2.75rem);
      line-height: 1.05;
      letter-spacing: -0.045em;
    }

    .clab__subtitle {
      margin: 0.65rem 0 0;
      font-size: 1rem;
      color: rgb(var(--reading-ink-secondary));
    }

    .clab__note {
      margin: 0.75rem 0 0;
      max-width: 44rem;
      font-size: 0.88rem;
      line-height: 1.65;
      color: rgb(var(--reading-ink-secondary));
    }

    .clab__meta {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(8rem, 1fr));
      gap: 0.65rem 1rem;
      margin: 1.15rem 0 0;
      padding: 0.85rem 1rem;
      border: 1px solid rgb(var(--border) / 0.22);
      border-radius: 8px;
      background: rgb(var(--reading-paper));
    }

    .clab__meta dt {
      font-size: 0.6rem;
      font-weight: 700;
      letter-spacing: 0.1em;
      text-transform: uppercase;
      color: rgb(var(--muted));
    }

    .clab__meta dd {
      margin: 0.15rem 0 0;
      font-size: 0.84rem;
    }

    .clab__criteria {
      margin-top: 1rem;
    }

    .clab__criteria-label {
      margin: 0;
      font-size: 0.62rem;
      font-weight: 700;
      letter-spacing: 0.1em;
      text-transform: uppercase;
      color: rgb(var(--muted));
    }

    .clab__criteria-list {
      display: flex;
      flex-wrap: wrap;
      gap: 0.4rem 0.65rem;
      margin: 0.45rem 0 0;
      padding: 0;
      list-style: none;
    }

    .clab__criteria-list li {
      padding: 0.25rem 0.55rem;
      border: 1px solid rgb(var(--border) / 0.35);
      border-radius: 999px;
      font-size: 0.72rem;
      color: rgb(var(--reading-ink-secondary));
      background: rgb(var(--reading-paper));
    }

    .clab__sets {
      display: flex;
      flex-direction: column;
      gap: 3rem;
      padding-top: 2rem;
    }

    .clab__set {
      scroll-margin-top: 4rem;
      padding-bottom: 2.5rem;
      border-bottom: 1px solid rgb(var(--border) / 0.2);
    }

    .clab__set:last-child {
      border-bottom: 0;
    }

    .clab__set-head h2 {
      margin: 0.2rem 0 0;
      font-family: var(--clab-display);
      font-size: clamp(1.25rem, 3vw, 1.65rem);
      letter-spacing: -0.03em;
      line-height: 1.12;
    }

    .clab__set-label {
      margin: 0;
      font-size: 0.62rem;
      font-weight: 700;
      letter-spacing: 0.12em;
      text-transform: uppercase;
      color: rgb(var(--muted));
    }

    .clab__set-role {
      margin: 0.45rem 0 0;
      max-width: 44rem;
      font-size: 0.86rem;
      line-height: 1.6;
      color: rgb(var(--reading-ink-secondary));
    }

    .clab__set-note {
      margin: 0.5rem 0 0;
      max-width: 44rem;
      padding: 0.5rem 0.75rem;
      border-left: 3px solid rgb(var(--border) / 0.45);
      font-size: 0.78rem;
      font-style: italic;
      line-height: 1.55;
      color: rgb(var(--muted));
    }

    .clab__panel {
      margin-top: 1.15rem;
    }

    .clab__panel-label {
      margin: 0 0 0.55rem;
      font-size: 0.58rem;
      font-weight: 700;
      letter-spacing: 0.1em;
      text-transform: uppercase;
      color: rgb(var(--muted));
    }

    .clab__swatches {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(7.5rem, 1fr));
      gap: 0.55rem;
      margin: 0;
      padding: 0;
      list-style: none;
    }

    .clab__swatch {
      border: 1px solid rgb(var(--border) / 0.22);
      border-radius: 6px;
      overflow: hidden;
      background: rgb(var(--reading-paper));
    }

    .clab__swatch-color {
      height: 2.75rem;
      border-bottom: 1px solid rgb(0 0 0 / 0.06);
    }

    .clab__swatch-meta {
      padding: 0.45rem 0.55rem;
    }

    .clab__swatch-meta strong {
      display: block;
      font-size: 0.68rem;
      font-weight: 600;
    }

    .clab__swatch-meta code {
      display: block;
      margin-top: 0.15rem;
      font-size: 0.62rem;
      color: rgb(var(--muted));
    }

    .clab__samples {
      display: grid;
      gap: 0.85rem;
      margin-top: 1rem;
    }

    .clab__kicker-sample {
      margin: 0 0 0.35rem;
      font-size: 0.62rem;
      font-weight: 700;
      letter-spacing: 0.12em;
      text-transform: uppercase;
    }

    .clab__h1 {
      margin: 0 0 0.45rem;
      font-family: var(--clab-display);
      font-size: clamp(1.35rem, 3.5vw, 1.85rem);
      font-weight: 650;
      letter-spacing: -0.04em;
      line-height: 1.1;
    }

    .clab__ingress {
      margin: 0;
      font-size: 0.9rem;
      line-height: 1.6;
    }

    .clab__ai {
      padding: 0.85rem 1rem;
      border-radius: 8px;
    }

    .clab__ai-label {
      margin: 0 0 0.3rem;
      font-size: 0.58rem;
      font-weight: 700;
      letter-spacing: 0.1em;
      text-transform: uppercase;
    }

    .clab__ai-title {
      margin: 0 0 0.25rem;
      font-size: 0.95rem;
      font-weight: 600;
    }

    .clab__ai-text {
      margin: 0 0 0.65rem;
      font-size: 0.85rem;
      line-height: 1.55;
    }

    .clab__ui-row {
      display: flex;
      flex-wrap: wrap;
      gap: 0.5rem;
      align-items: center;
      margin-bottom: 0.55rem;
    }

    .clab__btn {
      padding: 0.48rem 0.95rem;
      border-radius: 6px;
      font-family: var(--clab-sans);
      font-size: 0.84rem;
      font-weight: 600;
      cursor: default;
    }

    .clab__btn--secondary {
      background: transparent;
    }

    .clab__link {
      font-size: 0.84rem;
      font-weight: 600;
      text-decoration: underline;
      text-underline-offset: 2px;
    }

    .clab__pill {
      display: inline-flex;
      padding: 0.22rem 0.55rem;
      border-radius: 999px;
      font-size: 0.68rem;
      font-weight: 600;
      letter-spacing: 0.04em;
      text-transform: uppercase;
    }

    .clab__input {
      width: 100%;
      max-width: 20rem;
      padding: 0.5rem 0.75rem;
      border-radius: 6px;
      font-family: var(--clab-sans);
      font-size: 0.84rem;
    }

    .clab__input:focus {
      outline: 2px solid var(--clab-focus);
      outline-offset: 1px;
    }

    .clab__marker-demo {
      display: flex;
      align-items: center;
      gap: 0.85rem;
    }

    .clab__marker-line {
      display: block;
      width: 2.5rem;
      height: 3px;
      border-radius: 999px;
      background: var(--clab-signal);
    }

    .clab__marker-bars {
      display: flex;
      align-items: flex-end;
      gap: 3px;
      height: 1.25rem;
    }

    .clab__marker-bars span {
      display: block;
      width: 4px;
      border-radius: 2px;
      background: var(--clab-signal);
    }

    .clab__marker-bars span:nth-child(1) { height: 35%; }
    .clab__marker-bars span:nth-child(2) { height: 65%; }
    .clab__marker-bars span:nth-child(3) { height: 100%; }
    .clab__marker-bars span:nth-child(4) { height: 55%; }
    .clab__marker-bars span:nth-child(5) { height: 80%; }

    .clab__marker-dot {
      display: block;
      width: 8px;
      height: 8px;
      border-radius: 999px;
      background: var(--clab-signal);
    }

    .clab__marker-caption {
      margin: 0.45rem 0 0;
      font-size: 0.72rem;
      color: rgb(var(--muted));
    }

    .clab__hero-grid {
      display: grid;
      gap: 0.65rem;
    }

    .clab__hero {
      padding: 1rem 1.1rem;
      border-radius: 8px;
    }

    .clab__hero-tag {
      margin: 0 0 0.5rem;
      font-size: 0.55rem;
      font-weight: 700;
      letter-spacing: 0.1em;
      text-transform: uppercase;
    }

    .clab__hero-headline {
      margin: 0;
      font-family: var(--clab-display);
      font-size: clamp(1.15rem, 3vw, 1.45rem);
      font-weight: 650;
      letter-spacing: -0.035em;
      line-height: 1.1;
      max-width: 14em;
    }

    .clab__hero-subline {
      margin: 0.4rem 0 0;
      font-size: 0.78rem;
      line-height: 1.5;
      max-width: 22em;
    }

    /* ── Recommended hybrid candidate ── */
    .clab__hybrid {
      --hybrid-bg: #ffffff;
      --hybrid-text: #111827;
      --hybrid-muted: #4b5563;
      --hybrid-surface: #f3f4f6;
      --hybrid-border: #e5e7eb;
      --hybrid-deep: #0f4c81;
      --hybrid-teal: #0f4c5c;
      --hybrid-magenta: #ec4899;
      --hybrid-cyan: #0ea5e9;
      --hybrid-action: #111827;

      margin-top: 3rem;
      padding-top: 2.5rem;
      border-top: 2px dashed rgb(var(--border) / 0.45);
      color: var(--hybrid-text);
    }

    .clab__hybrid-head h2 {
      margin: 0.35rem 0 0;
      max-width: 44rem;
      font-family: var(--clab-display);
      font-size: clamp(1.45rem, 4vw, 2rem);
      letter-spacing: -0.04em;
      line-height: 1.08;
    }

    .clab__hybrid-head p:not(.clab__kicker) {
      margin: 0.65rem 0 0;
      max-width: 48rem;
      font-size: 0.9rem;
      line-height: 1.65;
      color: rgb(var(--reading-ink-secondary));
    }

    .clab__hybrid-head .clab__hybrid-usage-note {
      max-width: 46rem;
      padding: 0.65rem 0.8rem;
      border: 1px solid rgb(var(--border) / 0.28);
      border-left: 3px solid var(--hybrid-teal);
      border-radius: 8px;
      background: rgb(var(--surface-subtle) / 0.45);
      font-size: 0.82rem;
    }

    .clab__hybrid-tokens {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(9rem, 1fr));
      gap: 0.55rem;
      margin: 1rem 0 0;
      padding: 0;
      list-style: none;
    }

    .clab__hybrid-tokens li {
      padding: 0.6rem 0.7rem;
      border: 1px solid rgb(var(--border) / 0.24);
      border-radius: 8px;
      background: rgb(var(--reading-paper));
    }

    .clab__hybrid-tokens strong {
      display: block;
      font-size: 0.68rem;
      font-weight: 700;
      letter-spacing: 0.04em;
      text-transform: uppercase;
      color: rgb(var(--muted));
    }

    .clab__hybrid-tokens code {
      display: block;
      margin-top: 0.2rem;
      font-size: 0.68rem;
      color: var(--hybrid-text);
    }

    .clab__hybrid-rules {
      display: grid;
      gap: 0.75rem;
      margin-top: 1rem;
    }

    .clab__hybrid-rules > div {
      padding: 0.8rem 0.9rem;
      border: 1px solid rgb(var(--border) / 0.22);
      border-radius: 8px;
      background: rgb(var(--surface-subtle) / 0.45);
    }

    .clab__hybrid-rules ul {
      margin: 0;
      padding-left: 1.1rem;
      font-size: 0.82rem;
      line-height: 1.65;
      color: var(--hybrid-muted);
    }

    .clab__hybrid-grid {
      display: grid;
      gap: 0.85rem;
      margin-top: 1.25rem;
    }

    .clab__hybrid-panel {
      padding: 1rem;
      border: 1px solid var(--hybrid-border);
      border-radius: 10px;
      background: var(--hybrid-bg);
      color: var(--hybrid-text);
    }

    .clab__mock-chrome {
      display: flex;
      flex-wrap: wrap;
      gap: 0.75rem;
      align-items: center;
      justify-content: space-between;
      padding: 0.75rem;
      border: 1px solid var(--hybrid-border);
      border-radius: 8px;
      background: var(--hybrid-bg);
      box-shadow: inset 0 -2px 0 rgb(15 76 129 / 0.16);
    }

    .clab__wordmark {
      color: var(--hybrid-deep);
      font-size: 0.78rem;
      font-weight: 800;
      letter-spacing: 0.18em;
      text-decoration: none;
    }

    .clab__mock-nav {
      display: flex;
      flex-wrap: wrap;
      gap: 0.75rem;
      align-items: center;
      font-size: 0.78rem;
      color: var(--hybrid-muted);
    }

    .clab__mock-nav a {
      color: inherit;
      text-decoration: none;
    }

    .clab__mock-nav a:first-child {
      position: relative;
      color: var(--hybrid-text);
      text-decoration: underline;
      text-decoration-color: var(--hybrid-cyan);
      text-underline-offset: 4px;
    }

    .clab__mock-nav a:first-child::after {
      content: "";
      position: absolute;
      right: -0.35rem;
      top: -0.15rem;
      width: 5px;
      height: 5px;
      border-radius: 999px;
      background: var(--hybrid-magenta);
      box-shadow: 0 0 0 4px rgb(236 72 153 / 0.08);
    }

    .clab__mock-action {
      padding: 0.45rem 0.8rem;
      border: 0;
      border-radius: 999px;
      background: var(--hybrid-action);
      color: #ffffff;
      font-family: var(--clab-sans);
      font-size: 0.78rem;
      font-weight: 700;
    }

    .clab__editorial-start {
      position: relative;
      padding-left: 0.8rem;
    }

    .clab__edge-marker {
      position: absolute;
      left: 0;
      top: 0.15rem;
      width: 3px;
      height: 1.85rem;
      border-radius: 999px;
      background: var(--hybrid-magenta);
    }

    .clab__hybrid-editorial .clab__kicker-sample {
      color: var(--hybrid-deep);
    }

    .clab__hybrid-editorial .clab__ingress,
    .clab__hybrid-ai .clab__ai-text {
      color: var(--hybrid-muted);
    }

    .clab__hybrid-ai {
      border-color: rgb(255 255 255 / 0.08);
      background: #0a1628;
      color: #ffffff;
    }

    .clab__transition-line {
      width: 0.7rem;
      height: 0.7rem;
      margin-bottom: 0.75rem;
      border-radius: 999px;
      background: var(--hybrid-magenta);
      box-shadow: 0 0 0 6px rgb(236 72 153 / 0.1);
    }

    .clab__hybrid-ai .clab__ai-label {
      color: rgb(255 255 255 / 0.52);
    }

    .clab__hybrid-ai .clab__ai-text {
      color: rgb(255 255 255 / 0.68);
    }

    .clab__ai-form {
      display: grid;
      gap: 0.55rem;
      margin-top: 0.75rem;
    }

    .clab__hybrid .clab__input {
      max-width: none;
      border: 1px solid rgb(255 255 255 / 0.16);
      background: rgb(255 255 255 / 0.06);
      color: #ffffff;
    }

    .clab__hybrid .clab__input:focus {
      outline-color: var(--hybrid-cyan);
      box-shadow: 0 0 0 4px rgb(14 165 233 / 0.12), 0 0 18px rgb(236 72 153 / 0.1);
    }

    .clab__hybrid .clab__btn--primary {
      border: 0;
      background: #ffffff;
      color: var(--hybrid-action);
    }

    .clab__hub-cluster {
      display: grid;
      gap: 0.65rem;
    }

    .clab__hub-card {
      position: relative;
      padding: 0.9rem 1rem;
      border: 1px solid var(--hybrid-border);
      border-radius: 8px;
      background: var(--hybrid-bg);
    }

    .clab__hub-card > span {
      position: absolute;
      top: 0.85rem;
      right: 0.85rem;
      width: 7px;
      height: 7px;
      border-radius: 999px;
      background: var(--hybrid-cyan);
    }

    .clab__hub-card--active {
      border-color: rgb(15 76 129 / 0.28);
      box-shadow: inset 3px 0 0 var(--hybrid-deep);
    }

    .clab__hub-card--active > span {
      background: var(--hybrid-magenta);
    }

    .clab__hub-card h3 {
      margin: 0;
      max-width: 14rem;
      font-family: var(--clab-display);
      font-size: 1rem;
      font-weight: 650;
      letter-spacing: -0.03em;
      line-height: 1.12;
    }

    .clab__hub-card p {
      margin: 0.45rem 0 0;
      font-size: 0.8rem;
      line-height: 1.5;
      color: var(--hybrid-muted);
    }

    .clab__photo-card {
      display: grid;
      gap: 0.75rem;
    }

    .clab__photo-placeholder {
      min-height: 9rem;
      position: relative;
      overflow: hidden;
      border-radius: 8px;
      background:
        radial-gradient(circle at 22% 28%, rgb(255 255 255 / 0.85), transparent 24%),
        linear-gradient(135deg, #e5e7eb, #f9fafb 48%, #d1d5db);
    }

    .clab__photo-placeholder::before {
      content: "";
      position: absolute;
      inset: auto 0 0;
      height: 32%;
      background: linear-gradient(180deg, transparent, rgb(15 76 129 / 0.18));
    }

    .clab__photo-placeholder span {
      position: absolute;
      left: 1rem;
      bottom: 1rem;
      width: 0.65rem;
      height: 0.65rem;
      border-radius: 999px;
      background: var(--hybrid-magenta);
      box-shadow: 0 0 0 7px rgb(236 72 153 / 0.1);
    }

    .clab__photo-copy p {
      margin: 0 0 0.3rem;
      font-size: 0.58rem;
      font-weight: 700;
      letter-spacing: 0.1em;
      text-transform: uppercase;
      color: var(--hybrid-muted);
    }

    .clab__photo-copy strong {
      display: block;
      font-size: 0.86rem;
      line-height: 1.45;
    }

    .clab__signal-system {
      display: flex;
      flex-wrap: wrap;
      gap: 0.85rem;
      align-items: center;
    }

    .clab__hybrid-bars {
      display: flex;
      align-items: flex-end;
      gap: 4px;
      height: 2rem;
    }

    .clab__hybrid-bars span {
      display: block;
      width: 5px;
      border-radius: 999px;
      background: var(--hybrid-magenta);
    }

    .clab__hybrid-bars span:nth-child(1) { height: 35%; }
    .clab__hybrid-bars span:nth-child(2) { height: 70%; background: var(--hybrid-deep); }
    .clab__hybrid-bars span:nth-child(3) { height: 100%; }
    .clab__hybrid-bars span:nth-child(4) { height: 55%; background: var(--hybrid-cyan); }
    .clab__hybrid-bars span:nth-child(5) { height: 82%; }
    .clab__hybrid-bars span:nth-child(6) { height: 45%; background: var(--hybrid-deep); }

    .clab__dot-sequence {
      display: flex;
      gap: 0.35rem;
      align-items: center;
    }

    .clab__dot-sequence span {
      width: 7px;
      height: 7px;
      border-radius: 999px;
      background: var(--hybrid-border);
    }

    .clab__dot-sequence span:nth-child(2) {
      background: var(--hybrid-cyan);
    }

    .clab__dot-sequence span:nth-child(4) {
      background: var(--hybrid-magenta);
    }

    .clab__short-line {
      width: 2.5rem;
      height: 3px;
      border-radius: 999px;
      background: var(--hybrid-deep);
    }

    .clab__focus-ring {
      width: 1.65rem;
      height: 1.65rem;
      border: 2px solid var(--hybrid-cyan);
      border-radius: 999px;
      box-shadow: 0 0 0 5px rgb(14 165 233 / 0.12), 0 0 16px rgb(236 72 153 / 0.08);
    }

    .clab__ai-spark {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      width: 1.8rem;
      height: 1.8rem;
      border: 1px solid rgb(236 72 153 / 0.42);
      border-radius: 999px;
      color: var(--hybrid-magenta);
      font-size: 0.62rem;
      font-weight: 800;
      letter-spacing: 0.05em;
    }

    .clab__ghost-pill {
      padding: 0.4rem 0.7rem;
      border: 1px solid rgb(236 72 153 / 0.24);
      border-radius: 999px;
      background: transparent;
      color: var(--hybrid-deep);
      font-family: var(--clab-sans);
      font-size: 0.72rem;
      font-weight: 700;
      box-shadow: 0 0 0 4px rgb(236 72 153 / 0.05);
    }

    .clab__ghost-orb {
      width: 1.9rem;
      height: 1.9rem;
      border: 1px solid rgb(236 72 153 / 0.32);
      border-radius: 999px;
      background:
        radial-gradient(circle, rgb(236 72 153 / 0.24) 0 2px, transparent 3px),
        var(--hybrid-teal);
      box-shadow: 0 0 0 6px rgb(236 72 153 / 0.07), 0 0 18px rgb(236 72 153 / 0.1);
    }

    .clab__contrast-strip {
      display: grid;
      gap: 0.65rem;
    }

    .clab__contrast {
      padding: 1rem 1.1rem;
      border-radius: 8px;
    }

    .clab__contrast--light {
      border: 1px solid var(--hybrid-border);
      background: var(--hybrid-bg);
      color: var(--hybrid-text);
    }

    .clab__contrast--dark {
      border: 1px solid rgb(255 255 255 / 0.08);
      background: #0a1628;
      color: #ffffff;
    }

    .clab__contrast .clab__hero-headline {
      border-left: 3px solid var(--hybrid-deep);
      padding-left: 0.65rem;
    }

    .clab__contrast--dark .clab__hero-headline {
      border-left-color: rgb(255 255 255 / 0.16);
      text-shadow: 0 0 22px rgb(236 72 153 / 0.18);
    }

    .clab__contrast--dark .clab__hero-headline::after {
      content: "";
      display: inline-block;
      width: 0.42rem;
      height: 0.42rem;
      margin-left: 0.45rem;
      border-radius: 999px;
      background: var(--hybrid-magenta);
      box-shadow: 0 0 0 6px rgb(236 72 153 / 0.1);
      vertical-align: middle;
    }

    .clab__contrast--light .clab__hero-tag,
    .clab__contrast--light .clab__hero-subline {
      color: var(--hybrid-muted);
    }

    .clab__contrast--dark .clab__hero-tag,
    .clab__contrast--dark .clab__hero-subline {
      color: rgb(255 255 255 / 0.62);
    }

    /* ── Color hierarchy ── */
    .clab__hierarchy {
      --hier-deep: #0f4c5c;
      --hier-deep-dark: #071827;
      --hier-light: #ffffff;
      --hier-surface: #f3f4f6;
      --hier-ink: #111827;
      --hier-cyan: #0ea5e9;
      --hier-magenta: #ec4899;

      margin-top: 3rem;
      padding-top: 2.5rem;
      border-top: 2px dashed rgb(var(--border) / 0.45);
    }

    .clab__hierarchy-head h2 {
      margin: 0.35rem 0 0;
      max-width: 44rem;
      font-family: var(--clab-display);
      font-size: clamp(1.35rem, 3.5vw, 1.85rem);
      letter-spacing: -0.035em;
      line-height: 1.1;
    }

    .clab__hierarchy-head p:not(.clab__kicker) {
      margin: 0.65rem 0 0;
      max-width: 48rem;
      font-size: 0.88rem;
      line-height: 1.65;
      color: rgb(var(--reading-ink-secondary));
    }

    .clab__hierarchy-bar-wrap {
      margin-top: 1.25rem;
    }

    .clab__hierarchy-bar {
      display: flex;
      flex-direction: column;
      gap: 2px;
      min-height: 14rem;
      overflow: hidden;
      border: 1px solid rgb(var(--border) / 0.28);
      border-radius: 10px;
    }

    .clab__hierarchy-segment {
      flex: var(--seg-flex) 1 0;
      display: flex;
      flex-direction: column;
      justify-content: flex-end;
      gap: 0.15rem;
      min-height: 1.75rem;
      padding: 0.55rem 0.65rem;
      background: var(--seg-bg);
      color: var(--seg-text);
    }

    .clab__hierarchy-segment[data-segment="light"] {
      border: 1px solid #e5e7eb;
    }

    .clab__hierarchy-segment-share {
      font-size: 0.72rem;
      font-weight: 800;
      letter-spacing: 0.02em;
    }

    .clab__hierarchy-segment-label {
      font-size: 0.58rem;
      font-weight: 600;
      line-height: 1.35;
      opacity: 0.92;
    }

    .clab__hierarchy-segment[data-segment="magenta"] .clab__hierarchy-segment-label,
    .clab__hierarchy-segment[data-segment="cyan"] .clab__hierarchy-segment-label {
      writing-mode: vertical-rl;
      text-orientation: mixed;
      transform: rotate(180deg);
      font-size: 0.52rem;
    }

    .clab__hierarchy-parked {
      display: flex;
      flex-wrap: wrap;
      gap: 0.45rem;
      align-items: center;
      margin-top: 0.65rem;
    }

    .clab__hierarchy-chip {
      padding: 0.22rem 0.55rem;
      border-radius: 999px;
      background: var(--chip-bg);
      color: #ffffff;
      font-size: 0.62rem;
      font-weight: 700;
      letter-spacing: 0.04em;
      text-transform: uppercase;
      opacity: 0.72;
    }

    .clab__hierarchy-chip-note {
      font-size: 0.68rem;
      color: rgb(var(--muted));
      font-style: italic;
    }

    .clab__hierarchy-table-wrap {
      margin-top: 1.5rem;
    }

    .clab__hierarchy-table {
      display: grid;
      gap: 0.65rem;
    }

    .clab__hierarchy-row {
      padding: 0.85rem 1rem;
      border: 1px solid rgb(var(--border) / 0.22);
      border-radius: 8px;
      background: rgb(var(--reading-paper));
    }

    .clab__hierarchy-row--parked {
      opacity: 0.78;
      border-style: dashed;
    }

    .clab__hierarchy-row header {
      display: flex;
      flex-wrap: wrap;
      gap: 0.35rem 0.75rem;
      align-items: baseline;
      justify-content: space-between;
    }

    .clab__hierarchy-row h3 {
      margin: 0;
      font-size: 0.88rem;
      font-weight: 700;
    }

    .clab__hierarchy-share {
      margin: 0;
      font-size: 0.68rem;
      font-weight: 700;
      letter-spacing: 0.04em;
      text-transform: uppercase;
      color: rgb(var(--muted));
    }

    .clab__hierarchy-role {
      margin: 0.35rem 0 0.55rem;
      font-size: 0.8rem;
      line-height: 1.55;
      color: rgb(var(--reading-ink-secondary));
    }

    .clab__hierarchy-row dl {
      display: grid;
      gap: 0.45rem;
      margin: 0;
    }

    .clab__hierarchy-row dt {
      font-size: 0.58rem;
      font-weight: 700;
      letter-spacing: 0.08em;
      text-transform: uppercase;
      color: rgb(var(--muted));
    }

    .clab__hierarchy-row dd {
      margin: 0.12rem 0 0;
      font-size: 0.78rem;
      line-height: 1.55;
      color: rgb(var(--reading-ink-secondary));
    }

    .clab__hierarchy-row code {
      font-size: 0.72rem;
      color: rgb(var(--reading-ink));
    }

    .clab__hierarchy-mini-wrap {
      margin-top: 1.5rem;
    }

    .clab__hierarchy-mini-grid {
      display: grid;
      gap: 0.65rem;
    }

    .clab__hierarchy-mini {
      padding: 0.85rem 1rem;
      border-radius: 8px;
    }

    .clab__hierarchy-mini-label {
      margin: 0 0 0.45rem;
      font-size: 0.58rem;
      font-weight: 700;
      letter-spacing: 0.1em;
      text-transform: uppercase;
    }

    .clab__hierarchy-mini-kicker {
      margin: 0 0 0.25rem;
      font-size: 0.58rem;
      font-weight: 700;
      letter-spacing: 0.1em;
      text-transform: uppercase;
    }

    .clab__hierarchy-mini-headline {
      margin: 0;
      font-size: 0.95rem;
      font-weight: 700;
      line-height: 1.2;
    }

    .clab__hierarchy-mini-headline--display {
      font-family: var(--clab-display);
      font-size: 1.05rem;
      font-weight: 650;
      letter-spacing: -0.03em;
    }

    .clab__hierarchy-mini-copy {
      margin: 0.4rem 0 0;
      font-size: 0.72rem;
      line-height: 1.5;
    }

    .clab__hierarchy-mini--dark-hero {
      background: var(--hier-deep-dark);
      color: #ffffff;
    }

    .clab__hierarchy-mini--dark-hero .clab__hierarchy-mini-label {
      color: rgb(255 255 255 / 0.48);
    }

    .clab__hierarchy-mini--dark-hero .clab__hierarchy-mini-copy {
      color: rgb(255 255 255 / 0.62);
    }

    .clab__hierarchy-ghost-dot {
      display: inline-block;
      width: 0.42rem;
      height: 0.42rem;
      margin-left: 0.35rem;
      border-radius: 999px;
      background: var(--hier-magenta);
      box-shadow: 0 0 0 6px rgb(236 72 153 / 0.12);
      vertical-align: middle;
    }

    .clab__hierarchy-mini--editorial {
      border: 1px solid #e5e7eb;
      background: var(--hier-light);
      color: var(--hier-ink);
      box-shadow: inset 3px 0 0 var(--hier-deep);
    }

    .clab__hierarchy-mini--editorial .clab__hierarchy-mini-kicker {
      color: var(--hier-deep);
    }

    .clab__hierarchy-mini--editorial .clab__hierarchy-mini-copy {
      color: #4b5563;
    }

    .clab__hierarchy-mini--ai {
      background: var(--hier-surface);
      border: 1px solid #e5e7eb;
      color: var(--hier-ink);
    }

    .clab__hierarchy-mini--ai .clab__hierarchy-mini-kicker {
      color: #6b7280;
    }

    .clab__hierarchy-mini-signals {
      display: flex;
      gap: 0.35rem;
      margin: 0.45rem 0;
    }

    .clab__hierarchy-signal-dot {
      width: 0.45rem;
      height: 0.45rem;
      border-radius: 999px;
    }

    .clab__hierarchy-signal-dot--magenta {
      background: var(--hier-magenta);
      box-shadow: 0 0 0 4px rgb(236 72 153 / 0.08);
    }

    .clab__hierarchy-signal-dot--cyan {
      background: var(--hier-cyan);
      box-shadow: 0 0 0 4px rgb(14 165 233 / 0.1);
    }

    .clab__hierarchy-mini-btn {
      padding: 0.4rem 0.75rem;
      border: 0;
      border-radius: 6px;
      background: var(--hier-ink);
      color: #ffffff;
      font-family: var(--clab-sans);
      font-size: 0.72rem;
      font-weight: 700;
    }

    .clab__hierarchy-guardrail {
      margin: 1.25rem 0 0;
      max-width: 48rem;
      padding: 0.65rem 0.8rem;
      border-left: 3px solid var(--hier-deep);
      border-radius: 0 8px 8px 0;
      background: rgb(var(--surface-subtle) / 0.45);
      font-size: 0.82rem;
      line-height: 1.6;
      color: rgb(var(--reading-ink-secondary));
    }

    .clab__footer {
      margin-top: 2rem;
      padding-top: 1.25rem;
      border-top: 1px solid rgb(var(--border) / 0.18);
      font-size: 0.82rem;
      line-height: 1.65;
      color: rgb(var(--reading-ink-secondary));
    }

    .clab__footer p {
      margin: 0.35rem 0 0;
    }

    .clab__footer a {
      color: rgb(var(--reading-ink));
      font-weight: 600;
    }

    /* ── Per-set token application ── */
    .clab__set[data-set="a"] {
      --clab-bg: #ffffff;
      --clab-text: #111827;
      --clab-text-muted: #4b5563;
      --clab-surface: #f3f4f6;
      --clab-border: #e5e7eb;
      --clab-signal: #0f4c81;
      --clab-signal-secondary: #0ea5e9;
      --clab-focus: #0ea5e9;
      --clab-dark-bg: #0a1628;
      --clab-dark-text: #f9fafb;
      --clab-dark-muted: #9ca3af;
    }

    .clab__set[data-set="b"] {
      --clab-bg: #ffffff;
      --clab-text: #000000;
      --clab-text-muted: #374151;
      --clab-surface: #f4f5f7;
      --clab-border: #d1d5db;
      --clab-signal: #0055ff;
      --clab-signal-secondary: #10b981;
      --clab-focus: #0055ff;
      --clab-dark-bg: #0a0a0a;
      --clab-dark-text: #ffffff;
      --clab-dark-muted: #a3a3a3;
    }

    .clab__set[data-set="c"] {
      --clab-bg: #ffffff;
      --clab-text: #18181b;
      --clab-text-muted: #52525b;
      --clab-surface: #f4f4f5;
      --clab-border: #e4e4e7;
      --clab-signal: #ea580c;
      --clab-signal-secondary: #18181b;
      --clab-focus: #ea580c;
      --clab-dark-bg: #18181b;
      --clab-dark-text: #fafafa;
      --clab-dark-muted: #a1a1aa;
    }

    .clab__set[data-set="d"] {
      --clab-bg: #ffffff;
      --clab-text: #111827;
      --clab-text-muted: #4b5563;
      --clab-surface: #f3f4f6;
      --clab-border: #e5e7eb;
      --clab-signal: #ec4899;
      --clab-signal-deep: #0f4c5c;
      --clab-signal-secondary: #f97316;
      --clab-focus: #ec4899;
      --clab-dark-bg: #0a1418;
      --clab-dark-text: #f9fafb;
      --clab-dark-muted: #9ca3af;
    }

    /* Shared set styling */
    .clab__set .clab__article,
    .clab__set .clab__ui,
    .clab__set .clab__marker {
      padding: 0.85rem 1rem;
      border: 1px solid var(--clab-border);
      border-radius: 8px;
      background: var(--clab-bg);
      color: var(--clab-text);
    }

    .clab__set .clab__kicker-sample {
      color: var(--clab-signal);
    }

    .clab__set .clab__ingress,
    .clab__set .clab__ai-text {
      color: var(--clab-text-muted);
    }

    .clab__set .clab__ai {
      background: var(--clab-surface);
      border: 1px solid var(--clab-border);
      color: var(--clab-text);
    }

    .clab__set .clab__ai-label {
      color: var(--clab-text-muted);
    }

    .clab__set .clab__btn--primary {
      border: 0;
      background: var(--clab-text);
      color: var(--clab-bg);
    }

    .clab__set[data-set="c"] .clab__btn--primary {
      background: var(--clab-signal);
      color: #ffffff;
    }

    .clab__set .clab__btn--secondary {
      border: 1px solid var(--clab-border);
      color: var(--clab-text);
    }

    .clab__set .clab__link {
      color: var(--clab-signal);
    }

    .clab__set .clab__pill {
      border: 1px solid var(--clab-border);
      color: var(--clab-signal);
      background: var(--clab-bg);
    }

    .clab__set .clab__input {
      border: 1px solid var(--clab-border);
      background: var(--clab-bg);
      color: var(--clab-text);
    }

    .clab__set[data-set="d"] .clab__kicker-sample {
      color: var(--clab-signal-deep);
    }

    .clab__set[data-set="d"] .clab__link {
      color: var(--clab-signal-deep);
    }

    .clab__set[data-set="d"] .clab__pill {
      color: var(--clab-signal-deep);
      border-color: var(--clab-signal-deep);
    }

    .clab__set .clab__hero--light {
      background: var(--clab-bg);
      border: 1px solid var(--clab-border);
      color: var(--clab-text);
    }

    .clab__set .clab__hero--light .clab__hero-tag {
      color: var(--clab-text-muted);
    }

    .clab__set .clab__hero--light .clab__hero-subline {
      color: var(--clab-text-muted);
    }

    .clab__set .clab__hero--dark {
      background: var(--clab-dark-bg);
      border: 1px solid rgb(255 255 255 / 0.08);
      color: var(--clab-dark-text);
    }

    .clab__set .clab__hero--dark .clab__hero-tag {
      color: var(--clab-dark-muted);
    }

    .clab__set .clab__hero--dark .clab__hero-subline {
      color: var(--clab-dark-muted);
    }

    .clab__set[data-set="d"] .clab__hero--light .clab__hero-headline {
      border-left: 3px solid var(--clab-signal);
      padding-left: 0.65rem;
    }

    .clab__set[data-set="d"] .clab__hero--dark .clab__hero-headline {
      border-left: 3px solid var(--clab-signal);
      padding-left: 0.65rem;
    }

    @media (min-width: 640px) {
      [data-color-lab] {
        padding: 2rem 1.5rem 4rem;
      }

      .clab__samples {
        grid-template-columns: repeat(2, minmax(0, 1fr));
      }

      .clab__hero-pair {
        grid-column: 1 / -1;
      }

      .clab__hero-grid {
        grid-template-columns: repeat(2, minmax(0, 1fr));
      }

      .clab__hybrid-rules {
        grid-template-columns: repeat(2, minmax(0, 1fr));
      }

      .clab__hybrid-grid {
        grid-template-columns: repeat(2, minmax(0, 1fr));
      }

      .clab__hybrid-panel--wide {
        grid-column: 1 / -1;
      }

      .clab__mock-chrome {
        flex-wrap: nowrap;
      }

      .clab__ai-form {
        grid-template-columns: minmax(0, 1fr) auto;
        align-items: center;
      }

      .clab__hub-cluster {
        grid-template-columns: repeat(3, minmax(0, 1fr));
      }

      .clab__photo-card {
        grid-template-columns: minmax(0, 1.1fr) minmax(0, 0.9fr);
        align-items: center;
      }

      .clab__contrast-strip {
        grid-template-columns: repeat(2, minmax(0, 1fr));
      }

      .clab__hierarchy-bar {
        flex-direction: row;
        min-height: 9rem;
      }

      .clab__hierarchy-segment {
        min-height: auto;
        min-width: 0;
      }

      .clab__hierarchy-segment[data-segment="magenta"] .clab__hierarchy-segment-label,
      .clab__hierarchy-segment[data-segment="cyan"] .clab__hierarchy-segment-label {
        writing-mode: horizontal-tb;
        transform: none;
        font-size: 0.58rem;
      }

      .clab__hierarchy-table {
        grid-template-columns: repeat(2, minmax(0, 1fr));
      }

      .clab__hierarchy-row--parked {
        grid-column: 1 / -1;
      }

      .clab__hierarchy-mini-grid {
        grid-template-columns: repeat(3, minmax(0, 1fr));
      }
    }
  </style>
</PrototypeLayout>
