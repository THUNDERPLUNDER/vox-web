---
/* CONTRACT: VIS wireframe placeholder — artikkel; internal backstage only. */
import PrototypeLayout from "../../layouts/PrototypeLayout.astro";

const base = import.meta.env.BASE_URL;
---

<PrototypeLayout title="Artikkel">
  <main class="px-4 py-6 sm:px-6 sm:py-8">
    <h1 class="text-xl font-semibold text-gray-900">VIS wireframe – Artikkel</h1>
    <p class="mt-3 max-w-prose text-sm leading-relaxed text-gray-600">
      Placeholder. Bruk <a href={`${base}vis/`} class="text-gray-800 underline">oversikten</a> for rå HTML-wireframes.
    </p>
  </main>
</PrototypeLayout>
